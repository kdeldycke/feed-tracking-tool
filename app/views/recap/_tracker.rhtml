<!-- Partial affichant la liste des utilisateurs abonnés à un suivi -->

<div class="tracker">

<p><h4><%= Tracker.find_by_id(tracker.tracker_id).title %></h4></p>

<ul>
	<!-- Parcourt les utilisateurs abonnés au suivi courant -->
	<% Subscription.find(:all, :conditions => [ "tracker_id = ?", tracker.tracker_id ]).each do |j| -%>
	    <li><%= j.user_id %></li>	<!-- Affiche le nom de l'utilisateur -->
	<% end -%>
</ul>

</div>